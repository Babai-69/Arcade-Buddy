const fs = require('fs');
let code = fs.readFileSync('src/components/BadgeTracker.tsx', 'utf8');

// remove everything between first motion.div and end to test where the error is
// no, let me just fix it manually

console.log("no");

